.tabs-wrap .title {
  display: flex;
}
.tabs-wrap .title .tabs-item {
  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;
  padding: 15rpx 0;
}
.tabs-wrap .title .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
